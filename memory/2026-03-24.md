# 2026-03-24

- 为 `NeptuneLogQueue` 增加可选 SQLite 持久化模式，默认仍保持内存模式。
- 持久化实现采用 `GRDB` + `DatabaseQueue`，与现有 actor 串行访问模型一致。
- 持久化状态表保存 `nextRecordID`、`capacity`、`droppedOverflowCount`，确保重建实例后分页游标和 overflow 计数连续。
- 导出 API 保持兼容：`/v2/export/health`、`/v2/export/metrics`、`/v2/export/logs`。
- 为 `neptune-sdk-ios` 补充 GitHub Actions CI：`macos-15` 上执行 `xcrun swift test` 与 `./scripts/smoke-demo.sh`，覆盖 `main` push 和 pull request。
- 新增可执行 smoke demo：`NeptuneSDKiOSSmokeDemo` 与 `./scripts/smoke-demo.sh`，链路覆盖 SQLite 持久化、HTTP 导出、重开数据库校验和摘要输出。
- 已验证 `swift test` 与 smoke demo 脚本均通过，摘要会输出 `SMOKE_RESULT` 一行便于自动化抓取。
- `scripts/smoke-demo.sh` 增加 toolchain 兜底：优先 `xcrun --find swift`，无 `xcrun` 时再回退 `swift`，避免 `swiftly` 未选默认 toolchain 导致脚本失败。
